# Core Settings
StylesPath = styles
MinAlertLevel = suggestion
Packages = Google, proselint
Vocab = Syllog

# Global Scope Controls (Must be defined before syntax-specific sections)
IgnoredScopes = code, tt
IgnoredClasses = rust, rs

# ==============================================================================
# Documentation & Prose Markup (*.md, *.txt)
# ==============================================================================

[*.{md,txt}]
BasedOnStyles = Vale, Google, proselint

# Ignore YAML Frontmatter and LaTeX display/inline math blocks
BlockIgnores = (?s) (---.*?---), (?s) (\$\$.*?\$\$)
TokenIgnores = (\$.*?\$)

# Severity Overrides
Google.Passive = warning
Google.We = suggestion
Google.Wordiness = warning

# ==============================================================================
# Rust Source Code Comments (*.rs)
# ==============================================================================

[*.rs]
BasedOnStyles = Vale, Google

# Relax strict prose rules for technical code comments
Google.Passive = suggestion
Google.We = OFF
Google.Wordiness = suggestion