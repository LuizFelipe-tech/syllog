# rust-toolchain.toml
#
# Enforces Nightly toolchain usage for advanced formatting and linting features.

[toolchain]
channel = "nightly-2026-08-01"
components = ["rustfmt", "clippy"]
profile = "minimal"
targets = ["x86_64-pc-windows-gnu"]